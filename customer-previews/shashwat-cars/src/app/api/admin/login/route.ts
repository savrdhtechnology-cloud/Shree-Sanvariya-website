import { NextResponse } from "next/server";import { createSession, verifyAdminCredentials } from "@/lib/auth";export async function POST(req:Request){const body=await req.json().catch(()=>({}));const email=String(body.email||"");const password=String(body.password||"");if(!verifyAdminCredentials(email,password))return NextResponse.json({error:"Invalid credentials or admin authentication is not configured."},{status:401});await createSession(email);return NextResponse.json({ok:true})}
