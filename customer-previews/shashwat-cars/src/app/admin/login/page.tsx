"use client";import { FormEvent,useState } from "react";import { useRouter } from "next/navigation";export default function Login(){const [error,setError]=useState("");const router=useRouter();async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");const d=Object.fromEntries(new FormData(e.currentTarget).entries());const r=await fetch("/api/admin/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(d)});if(r.ok){router.push("/admin");router.refresh()}else setError("Login failed. Configure ADMIN_EMAIL and ADMIN_PASSWORD_SHA256 in the environment.")}return <section className="section container admin-login"><form className="card form-card" onSubmit={submit}><p className="eyebrow">SECURE ADMIN</p><h1>Shashwat Cars Login</h1><label>Email<input name="email" type="email" required/></label><label>Password<input name="password" type="password" required/></label><button className="btn btn-accent">Login</button>{error&&<p className="error">{error}</p>}</form></section>}
