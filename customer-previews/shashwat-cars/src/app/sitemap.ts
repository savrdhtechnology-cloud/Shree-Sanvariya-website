import type { MetadataRoute } from "next";import { business } from "@/lib/config";import { getVehicles } from "@/lib/db";export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=business.siteUrl;const staticPaths=["","/cars","/sell-your-car","/finance","/about","/reviews","/contact","/privacy-policy","/terms-and-conditions","/disclaimer"];const cars=(await getVehicles()).filter(v=>v.status!=="HIDDEN");return [...staticPaths.map(p=>({url:`${base}${p}`,lastModified:new Date(),changeFrequency:"weekly" as const,priority:p===""?1:0.7})),...cars.map(v=>({url:`${base}/cars/${v.slug}`,lastModified:v.createdAt?new Date(v.createdAt):new Date(),changeFrequency:"daily" as const,priority:0.8}))]}
