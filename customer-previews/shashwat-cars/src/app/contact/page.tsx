import { LeadForm } from "@/components/LeadForm";import { business, phoneHref, whatsappHref } from "@/lib/config";export default function Page(){return <section className="section container"><div className="page-title"><p className="eyebrow">CONTACT</p><h1>Visit Shashwat Cars</h1><p>Connect with the dealership for inventory, valuation, inspection or finance enquiries.</p></div><div className="contact-grid"><div className="card contact-card"><h2>{business.name}</h2><p>{business.address}</p>{business.phone&&<a href={phoneHref()}>{business.phone}</a>}{business.email&&<a href={`mailto:${business.email}`}>{business.email}</a>}<a href={business.mapsUrl} target="_blank">Google Maps / Directions</a><a href={whatsappHref("Hello, I have an enquiry for Shashwat Cars.")}>WhatsApp</a></div><div className="card form-card"><h2>Send an Enquiry</h2><LeadForm type="GENERAL"/></div></div></section>}
